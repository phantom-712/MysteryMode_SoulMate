Virtual Environment
This folder contains libraries specific to your machine and can be very large.

venv/
Python cache files
These are temporary files generated by Python that don't need to be shared.

pycache/
*.pyc
Database file
Each user will create their own local database. Sharing this can cause conflicts.

database.db
User uploaded files
Do not upload personal photos or audio messages to the public repository.

static/uploads/
Instance folder (can sometimes contain sensitive config)

instance/
